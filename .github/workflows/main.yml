# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [main]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    name: Build
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: executing ssh commands using ssh key
        env:
          AWS_EC2_PEM: ${{ secrets.AWS_EC2_PEM  }}
          AWS_EC2_PUBLIC_IP: ${{ secrets.AWS_EC2_PUBLIC_IP}}
          AWS_EC2_USERNAME: ${{ secrets.AWS_EC2_USERNAME}}
        run: |
            echo 'ok'
            echo 'o0o'
            $AWS_EC2_PEM > nadiakey.pem && chmod 600 nadiakey.pem
            echo 'ok 2'
            $AWS_EC2_PUBLIC_IP > AWS_EC2_PUBLIC_IP
            $AWS_EC2_USERNAME > AWS_EC2_USERNAME
            echo 'aaa1'
            echo $nadiakey.pem
            echo 'nadiakey.pem'
            ssh -i nadiakey.pem ubuntu@13.38.2.240
            echo 'second ok'
            rm -rf FastAPI
            git clone git@github.com:NadiaBensalah/FastAPI.git
            cd FastAPI
            sudo docker-compose build
            sudo docker-compose up -d
